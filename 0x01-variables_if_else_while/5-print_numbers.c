#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all single digits of base 10
 * Return: 0 (Success)
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');

	return (0);
}
